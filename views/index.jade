!!!5
html(lang='en')
  head
    title= title
    script(src='/yui3?build/yui/yui-min.js')
    script
      YUI({
        comboBase: '/yui3?',
        combine: true,
        root: 'build/',
        groups: {
          contrib: {
            base: '/yui3-contrib?build/',
            // explicitly list the modules and deps, this improves combohandler bundling of
            // script requests
            modules: {
              'my-module-bar': {
                requires: ['my-module-bar']
              },
              'my-module-foo': {
              }
            },
            /**
            // pattern based module loading, combohandler won't group these
            patterns: {
              'my-': {},
              'mycss-': { type: 'css' }
            }
            **/
          }
        }
      }).use('my-module-bar', function (Y) {
      });
  body
    h1= title 
    p check the console...
